//
// Visibility
//

// Visibility Type
type Visibility = {
  visibility: {
    visible: string;
    invisible: string;
  },
};

// Visibility Utility
const VISIBILITY: Visibility = {
  visibility: {
    visible: 'visible',
    invisible: 'invisible',
  },
};

export default VISIBILITY;
