/* eslint-disable @typescript-eslint/no-var-requires */
const Path = require("path");
const createFileTypes = require("../../../util/createFileTypes");
const Display = require("../../Variabel/utilities/Display");

// //////////////////////////////////////////////////////////////////
// Display //////////////////////////////////////////////////////////
// //////////////////////////////////////////////////////////////////
const DisplayObj = {};

Object.assign(DisplayObj, {
  Display: Display[""],
  DisplaySM: Display.sm,
  DisplayMD: Display.md,
  DisplayLG: Display.lg,
  DisplayXL: Display.xl,
  DisplayPrint: Display.print,
});

// //////////////////////////////////////////////////////////////////
// Create File types ////////////////////////////////////////////////
// //////////////////////////////////////////////////////////////////
createFileTypes(DisplayObj, Path.resolve(__dirname, "types.tsx"));
