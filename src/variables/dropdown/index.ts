import {
  Dropdown_,
  DropdownMenu_,
  DropdownItem_,
} from './type';
import { GLOBAL } from '../global';

// Dropdown variables
const DROPDOWN = {
  ...GLOBAL,
  base: 'dropdown',
};

// Dropdown Menu variables
const DROPDOWN_MENU = {
  ...GLOBAL,
  base: 'dropdown-menu',
};

// Dropdown Item variables
const DROPDOWN_ITEM = {
  ...GLOBAL,
  base: 'dropdown-item',
};

export {
  DROPDOWN,
  type Dropdown_,
  DROPDOWN_MENU,
  type DropdownMenu_,
  DROPDOWN_ITEM,
  type DropdownItem_,
};
