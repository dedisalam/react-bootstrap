import {
  NavItem_,
} from './type';
import { GLOBAL } from '../global';

// Nav Item variables
const NAV_ITEM = {
  ...GLOBAL,
  base: 'nav-item',
  dropdown: 'dropdown',
};

export {
  NAV_ITEM,
  type NavItem_,
};
